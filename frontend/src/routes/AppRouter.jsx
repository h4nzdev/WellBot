import { BrowserRouter as Router } from "react-router-dom";

import Index from "./Index";

const AppRouter = () => {
  return (
    <Router>
      <Index />
    </Router>
  );
};

export default AppRouter;
